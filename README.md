# Sunnyside — Agency landing page

Solution to the Frontend Mentor [Sunnyside agency landing page challenge](https://www.frontendmentor.io/challenges/agency-landing-page-7yVs3B6ef).

## Overview

Responsive landing page built from the provided design files. The solution follows a mobile-first workflow and includes a small JavaScript helper for the mobile navigation.

### Screenshot

<img src="./screenshot.jpg" alt="preview">

### Links

- Solution (this repo): https://github.com/pettik/FrontendMentor--agency-landing-page
- Live site: (add your deployed URL here)

## How to run locally

Open `index.html` in your browser, or use a simple static server for a local URL. Examples:

```bash
# Serve locally using a static server (example with `live-server`):
live-server --port=3000
```

Then open http://localhost:3000 in your browser.

## Built with

- Semantic HTML5
- CSS custom properties (variables)
- Flexbox
- CSS Grid
- Mobile-first responsive design
- Vanilla JavaScript (menu toggle)

## What I learned

- Building a mobile-first responsive layout and switching image sources with `picture`/`source`.
- Implementing an accessible mobile navigation (aria-label updates and keyboard-friendly markup).
- Using CSS custom properties to manage typography and spacing across breakpoints.

## Continued development

- Improve keyboard interaction for the mobile menu (focus trap / escape to close).
- Add progressive enhancement for image loading (loading=lazy where appropriate).

## Useful resources

- Frontend Mentor challenge page — the project spec and assets
- MDN Web Docs — responsive images and `picture` element
- A11y project notes for accessible navigation

## AI Collaboration

This repository and `README.md` were reviewed and updated locally; minimal automated assistance was used to draft this README.

## Author

- Petr Bednarski — https://github.com/pettik

## Notes for submission

1. Add a `screenshot.jpg` in the repo root (or replace the preview image) to show your solution in the GitHub preview.
2. Deploy the project (GitHub Pages / Netlify / Vercel) and paste the live URL in the Links section above.
3. When submitting to Frontend Mentor, include the GitHub repo URL and the live site URL.

Good luck with your submission! 🎉
